{
  "name": "p12",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "concurrently \"mongod\" \"cd back && nodemon \" \"cd front && npm start\"",
    "start": "cd backend && npm start",
    "start:dev": "concurrently \"cd back && nodemon\" \"cd front && npm start\" ",
    "start:build": "cd front && npm run build && cd ../backend && npm start",
    "heroku-postbuild": "cd front && npm i && npm run build && cd ../backend && npm i"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^3.3.0-beta2"
  },
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
